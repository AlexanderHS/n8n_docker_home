# Cloudflare credentials for Let's Encrypt certificate generation
# Option 1: API Token (preferred)
CF_DNS_API_TOKEN=your_cloudflare_api_token_here

# Option 2: Legacy Global API Key (if token doesn't work with subdomains)
# Get from: https://dash.cloudflare.com/profile/api-tokens (Global API Key)
CF_API_EMAIL=your_cloudflare_email@example.com
CF_API_KEY=your_global_api_key_here